---
description: Write a PRD entry for a new feature (strategist hat, main loop)
argument-hint: <feature name> — <one-line idea>
---

Wear the **strategist hat** (`docs/PRINCIPLES.md § Role hats`). Write a PRD entry for: $ARGUMENTS

- Reject the feature if it serves none of the MVP pillars in `docs/PRD.md`. Say so plainly and stop. Default bias: CUT / POST-MVP — the feature must earn its way INTO v1.
- If it might be MVP-fit, propose the smallest version that still delivers the pillar outcome.
- Format: **Feature / Pillar / Status (in-MVP | post-MVP | cut) / User story / Acceptance criteria (incl. empty + offline + error) / Out of scope**.
- Append under the matching pillar in `docs/PRD.md`. Don't duplicate.
- Output: the appended block only. No preamble, no summary.
