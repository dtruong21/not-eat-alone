---
description: Explain a non-obvious piece of code in ≤100 words
argument-hint: <file path>[:line] or <symbol>
---

Explain: $ARGUMENTS

Constraints:
- Read only the file in question (and its direct imports if needed for context). Do not roam.
- Output ≤100 words.
- Lead with the WHY (the non-obvious reason this code exists), not the WHAT.
- If the code IS obvious, say so in one line and stop.

No code blocks unless quoting ≤3 lines is essential.
