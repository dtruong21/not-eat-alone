/// Hosted legal document URLs, linked from `SettingsScreen`.
///
/// Placeholders — swap for the real hosted URLs before store submission.
library;

const privacyPolicyUrl = 'https://convyve.com/privacy';
const termsOfServiceUrl = 'https://convyve.com/terms';
